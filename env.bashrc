#!/bin/bash
SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
export PO_SRC=${SCRIPT_DIR}
export PO_ALGOS=${SCRIPT_DIR}/Algorithms
export PO_BENCHS=${SCRIPT_DIR}/Benchmarks
